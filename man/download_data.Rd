% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{download_data}
\alias{download_data}
\title{Load base‑rate database, similarity matrices or human validation ratings from package}
\usage{
download_data(
  which = c("database", "validation_ratings", "similarity_matrix_gpt4",
    "similarity_matrix_llama3.3"),
  dest = NULL
)
}
\arguments{
\item{which}{One of "database", "validation_ratings", "similarity_matrix_gpt4", or "similarity_matrix_llama3.3".}

\item{dest}{Optional path to copy the file to (returns the data either way).}
}
\value{
A tibble with the requested data.
}
\description{
The "database" contains all base-rate items with stereotype strengths
from GPT-4 and LLaMA 3.3.
}
\details{
The "validation_ratings" contains the average typicality ratings from 50 participants
for 100 group–adjective pairs, as well as the ratings by GPT-4 and LLaMA 3.3.

The "similarity_matrix_gpt4" and "similarity_matrix_llama3.3" are raw typicality matrices
generated by each respective model.
}
\examples{
# Load the full base-rate database (LLM-generated stereotype strengths)
database <- download_data("database")

# Load validation ratings (human, GPT-4, LLaMA 3.3) for 100 items
ratings <- download_data("validation_ratings")

# Load the raw typicality matrix from GPT-4
gpt4_matrix <- download_data("similarity_matrix_gpt4")

# Load the raw typicality matrix from LLaMA 3.3
llama3_matrix <- download_data("similarity_matrix_llama3.3")
}
